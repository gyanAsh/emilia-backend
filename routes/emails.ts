// import { Router } from "../deps.ts";
// import { getEmails, getEmailById } from "../controllers/emails.ts";
// import { authMiddleware } from "../middlewares/auth.ts";

// const router = new Router();

// router
//   .get("/emails/:folder?", authMiddleware, getEmails)
//   .get("/emails/view/:id", authMiddleware, getEmailById);

// export default router;
